---
phase: 260529-d2g-create-adjust-tmux-for-darwin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - home/.chezmoidata/darwin/packages.toml
  - home/private_dot_config/tmux/tmux.conf.tmpl
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "tmux is installed via Homebrew on macOS"
    - "tmux.conf.tmpl works on macOS without Linux-only dependencies"
    - "Resurrect plugin hooks do not fail on macOS"
  artifacts:
    - path: "home/.chezmoidata/darwin/packages.toml"
      provides: "tmux in Homebrew packages"
      contains: "'tmux'"
    - path: "home/private_dot_config/tmux/tmux.conf.tmpl"
      provides: "Darwin-compatible resurrect hooks"
      contains: "xdotool"
  key_links:
    - from: "home/.chezmoidata/darwin/packages.toml"
      to: "home/private_dot_config/homebrew/Brewfile.tmpl"
      via: "chezmoi template data"
      pattern: "packages\\.homebrew\\.brew"
---

<objective>
Make tmux configuration fully functional on macOS (Darwin) by adding tmux to Homebrew packages and fixing Linux-only resurrect hooks that would fail on macOS.

Purpose: Ensure the dotfiles repo provides a working tmux setup on macOS without manual intervention.
Output: Updated packages.toml with tmux, and tmux.conf.tmpl with Darwin-compatible resurrect hooks.
</objective>

<execution_context>
@/Users/fabiosouzadev/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fabiosouzadev/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From home/.chezmoidata/darwin/packages.toml:
```toml
[packages.homebrew]
brew = [
    'lazygit',
    'lazyjournal',
    'atuin'
]
```

From home/private_dot_config/tmux/tmux.conf.tmpl (lines 252-253):
```
set -g @resurrect-hook-post-save-all 'eval $(xdotool getwindowgeometry --shell $WINDOWID); echo 0,$X,$Y,$WIDTH,$HEIGHT > $HOME/.local/share/tmux/resurrect/geometry'
set -g @resurrect-hook-pre-restore-all 'wmctrl -i -r $WINDOWID -e $(cat $HOME/.local/share/tmux/resurrect/geometry)'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tmux to Homebrew packages for Darwin</name>
  <files>home/.chezmoidata/darwin/packages.toml</files>
  <action>Add 'tmux' to the brew list in home/.chezmoidata/darwin/packages.toml. The brew list currently contains lazygit, lazyjournal, and atuin. Append 'tmux' to this list. This ensures chezmoi will install tmux via Homebrew when applying dotfiles on macOS.</action>
  <verify>
    <automated>grep -q "'tmux'" /Users/fabiosouzadev/.local/share/chezmoi/home/.chezmoidata/darwin/packages.toml && echo "PASS: tmux found in brew list" || echo "FAIL: tmux not found"</automated>
  </verify>
  <done>tmux appears in the brew list in packages.toml</done>
</task>

<task type="auto">
  <name>Task 2: Fix resurrect hooks for Darwin compatibility</name>
  <files>home/private_dot_config/tmux/tmux.conf.tmpl</files>
  <action>Wrap the resurrect geometry hooks (lines 252-253) in a platform check so they only run on Linux. The xdotool and wmctrl commands are Linux-only and will fail on macOS. Use tmux's if-shell to check the OS before executing these hooks. The check should test if the commands exist before running them. Specifically, change the two set -g lines to use if-shell guards that check for xdotool/wmctrl availability, falling back to no-op on macOS.</action>
  <verify>
    <automated>grep -A5 "resurrect-hook" /Users/fabiosouzadev/.local/share/chezmoi/home/private_dot_config/tmux/tmux.conf.tmpl | grep -q "if-shell" && echo "PASS: hooks are guarded" || echo "FAIL: hooks not guarded"</automated>
  </verify>
  <done>Resurrect hooks are wrapped in if-shell guards that check for xdotool/wmctrl before executing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chezmoi apply → scripts | Scripts execute with user privileges during dotfiles setup |
| Homebrew → package install | External package manager installs binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260529-01 | Tampering | Homebrew packages.toml | accept | Low risk — local config file, not fetched from remote |
| T-260529-02 | Information Disclosure | tmux.conf.tmpl | accept | No secrets in tmux config — only keybindings and theme settings |
</threat_model>

<verification>
- tmux is listed in the brew packages in packages.toml
- Resurrect hooks are guarded with if-shell checks for xdotool/wmctrl
- tmux.conf.tmpl remains valid tmux configuration syntax
</verification>

<success_criteria>
1. `grep -q "'tmux'" home/.chezmoidata/darwin/packages.toml` returns true
2. Resurrect hooks in tmux.conf.tmpl use if-shell guards for Linux-only commands
3. Configuration is valid tmux syntax (no broken template rendering)
</success_criteria>

<output>
Create `.planning/quick/260529-d2g-create-adjust-tmux-for-darwin/260529-d2g-01-SUMMARY.md` when done
</output>

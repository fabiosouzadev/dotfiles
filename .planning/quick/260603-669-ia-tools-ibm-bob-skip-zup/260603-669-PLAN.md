---
phase: quick-260603-669
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - home/.chezmoidata/ai_tools.yaml
  - home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl
autonomous: true
requirements: [QT-260603-669]
must_haves:
  truths:
    - "Em workplace Zup, a instalação de bobibm é pulada (não aparece no bash gerado)."
    - "Em workplaces diferentes de Zup (none/pessoal, work não-Zup), bobibm é instalado normalmente."
    - "Tools sem o campo block_on_workplace continuam instalando em qualquer workplace (sem regressão)."
    - "O guard é declarativo e genérico — qualquer tool (npm ou curl) com block_on_workplace é respeitada."
    - "Ambientes efêmeros continuam pulando todas as tools (comportamento existente inalterado)."
  artifacts:
    - path: "home/.chezmoidata/ai_tools.yaml"
      provides: "Campo declarativo block_on_workplace: Zup na entrada bobibm"
      contains: "block_on_workplace: Zup"
    - path: "home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl"
      provides: "Guard $blocked respeitando block_on_workplace nos ranges npm e curl"
      contains: "block_on_workplace"
  key_links:
    - from: "home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl"
      to: "home/.chezmoidata/ai_tools.yaml (.block_on_workplace)"
      via: "hasKey . block_on_workplace no range"
      pattern: "hasKey . \"block_on_workplace\""
    - from: "home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl"
      to: ".settings.workplace (raiz via $)"
      via: "eq .block_on_workplace $.settings.workplace"
      pattern: "eq .block_on_workplace \\$.settings.workplace"
---

<objective>
Adicionar um guard declarativo e reutilizável que bloqueia a instalação de uma AI tool em um
workplace específico, e aplicá-lo à entrada `bobibm` para que ela seja instalada em todos os
ambientes EXCETO Zup.

Purpose: A `bob.ibm.com` é um instalador interno que não pode rodar na máquina corporativa Zup,
mas deve continuar disponível nos demais ambientes (pessoal/none, work não-Zup). A solução é
declarativa (campo no YAML) e genérica (qualquer tool pode usá-la), não um hardcode de `bobibm`.

Output:
- Campo `block_on_workplace: Zup` na entrada `bobibm` de `home/.chezmoidata/ai_tools.yaml`.
- Guard `$blocked` nos ranges npm e curl de `run_once_after_510-install-ai-tools.sh.tmpl`,
  combinado com `$featureEnabled` no `if` de instalação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260603-669-ia-tools-ibm-bob-skip-zup/260603-669-CONTEXT.md
@.planning/quick/260603-669-ia-tools-ibm-bob-skip-zup/260603-669-RESEARCH.md
@home/.chezmoidata/ai_tools.yaml
@home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl

# Idiomas Go-template/sprig já comprovados no próprio script:
#   - Campo da tool dentro do range: .feature_flag, .name, .url
#   - Dado da raiz dentro do range: $.features.ai.coding_assistants, $.settings.workplace
#   - Campo opcional: hasKey . "env", hasKey . "completions_cmd"
# Origem de .settings.workplace: home/.chezmoi.yaml.tmpl:241
#   (ternary "Zup" "none" (eq $profile "work"))
# Detecção comprovada via chezmoi execute-template (durante planning):
#   workplace=Zup  -> bobibm BLOQUEADO (só claude/outros instalam)
#   workplace=none -> bobibm INSTALA normalmente
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo declarativo block_on_workplace na entrada bobibm</name>
  <files>home/.chezmoidata/ai_tools.yaml</files>
  <action>
Na entrada `bobibm` dentro de `ai_tools.curl` (atualmente linhas 54-57), adicionar a linha
`block_on_workplace: Zup` como novo campo da tool, mantendo a indentação dos campos irmãos
(`url`, `shell`, `feature_flag` — 6 espaços de indentação). Estado final da entrada:

    - name: bobibm
      url: "https://bob.ibm.com/download/bobshell.sh"
      shell: bash
      feature_flag: coding_assistants
      block_on_workplace: Zup

O valor `Zup` casa exatamente com `$.settings.workplace` quando o perfil é work-Zup
(`ternary "Zup" "none" ...` em home/.chezmoi.yaml.tmpl:241). NÃO adicionar o campo em nenhuma
outra entrada — apenas bobibm. NÃO alterar as linhas em branco finais do arquivo. Este é o único
guard declarativo; não há hardcode de `bobibm` no script (per CONTEXT: campo genérico reutilizável).
  </action>
  <verify>
    <automated>cd /home/fabio.souza/.local/share/chezmoi && python3 -c "import yaml; d=yaml.safe_load(open('home/.chezmoidata/ai_tools.yaml')); b=[t for t in d['ai_tools']['curl'] if t['name']=='bobibm'][0]; assert b.get('block_on_workplace')=='Zup', 'bobibm sem block_on_workplace=Zup'; assert all('block_on_workplace' not in t for t in d['ai_tools']['curl'] if t['name']!='bobibm'), 'campo vazou para outra curl tool'; assert all('block_on_workplace' not in t for t in d['ai_tools']['npm']), 'campo vazou para npm tool'; print('OK: block_on_workplace=Zup apenas em bobibm')"</automated>
  </verify>
  <done>
`ai_tools.yaml` é YAML válido; a entrada `bobibm` contém `block_on_workplace: Zup`; nenhuma
outra entrada (npm ou curl) possui o campo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aplicar guard $blocked nos ranges npm e curl do script de instalação</name>
  <files>home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl</files>
  <action>
Adicionar, em AMBOS os ranges (`.ai_tools.npm` e `.ai_tools.curl`), o cálculo de uma variável
`$blocked` espelhando o estilo de `$featureEnabled`, e combiná-la no `if` de instalação.

No range NPM: após a linha que monta `$featureEnabled` para `spec_tools` (atual linha 13) e
antes do `{{ if $featureEnabled }}` (atual linha 15), inserir duas linhas de lógica usando
hífens nos dois lados (forma `{{- ... -}}`) para NÃO injetar linha em branco no bash final —
declarar `$blocked := false` e setá-lo `true` quando, dentro de um único `and`, primeiro
`hasKey . "block_on_workplace"` e depois `eq .block_on_workplace $.settings.workplace`. Em
seguida trocar `{{ if $featureEnabled }}` por `{{ if and $featureEnabled (not $blocked) }}`
(sem hífen à esquerda, preservando a quebra de linha que separa o bloco — espelhar exatamente o
padrão da linha atual). Não tocar nos `{{- end }}` existentes do range.

No range CURL: aplicar a MESMA inserção — declarar `$blocked` após a linha de `$featureEnabled`
spec_tools (atual linha 30) e trocar `{{ if $featureEnabled }}` (atual linha 32) por
`{{ if and $featureEnabled (not $blocked) }}`. Não tocar nos `{{- end }}` existentes.

A ordem `hasKey` ANTES de `eq` dentro do `and` é obrigatória: o short-circuit de `and` (avaliado
da esquerda p/ direita) garante que `eq .block_on_workplace ...` nunca compare contra `<no value>`
para tools sem o campo. NÃO usar guard hardcoded para `bobibm` — a checagem é genérica por campo
(per CONTEXT). O snippet exato recomendado está em RESEARCH.md seções 3-4 (variável `$blocked`).
NÃO alterar os templates de topo (`script_is_not_ephemeral`, `script_eval_mise`,
`script_helper`) — efêmeros já estão cobertos.
  </action>
  <verify>
    <automated>bash /home/fabio.souza/.local/share/chezmoi/.planning/quick/260603-669-ia-tools-ibm-bob-skip-zup/verify-task2.sh</automated>
  </verify>
  <done>
Guard `$blocked` presente nos dois ranges; `chezmoi execute-template` renderiza sem erro; em
workplace Zup `bobibm` NÃO aparece no bash gerado e `claude`/`aider`/`opencode` continuam; em
workplace simulado não-Zup `bobibm` é instalado. Nenhuma linha em branco extra injetada entre tools.
  </done>
</task>

</tasks>

<verification>
Antes de marcar concluído, o executor deve criar o script de verificação
`.planning/quick/260603-669-ia-tools-ibm-bob-skip-zup/verify-task2.sh` (referenciado no `<verify>`
da Task 2) e torná-lo executável. Ele exercita a renderização real do template (workplace atual =
Zup) e uma renderização inline simulando workplace não-Zup. Conteúdo do script:

    #!/usr/bin/env bash
    set -euo pipefail
    cd /home/fabio.souza/.local/share/chezmoi
    F=home/.chezmoiscripts/unix/run_once_after_510-install-ai-tools.sh.tmpl

    echo "--- 1) guard presente nos dois ranges ---"
    [ "$(grep -c 'block_on_workplace' "$F")" -ge 2 ] || { echo "FAIL: guard nao aplicado nos dois ranges"; exit 1; }
    [ "$(grep -c 'and \$featureEnabled (not \$blocked)' "$F")" -eq 2 ] || { echo "FAIL: if combinado ausente em algum range"; exit 1; }

    echo "--- 2) template real renderiza sem erro (workplace atual = Zup) ---"
    OUT_REAL="$(chezmoi execute-template < "$F")"
    echo "$OUT_REAL" | grep -q 'AI Tools Installed' || { echo "FAIL: render quebrado"; exit 1; }

    echo "--- 3) workplace=Zup EXCLUI bobibm ---"
    if echo "$OUT_REAL" | grep -q 'bobibm'; then echo "FAIL: bobibm ainda renderiza em Zup"; exit 1; fi
    echo "$OUT_REAL" | grep -q 'Installing claude (Curl)' || { echo "FAIL: claude sumiu (regressao)"; exit 1; }

    echo "--- 4) workplace!=Zup INCLUI bobibm (simulado inline) ---"
    SNIP='{{- range (list (dict "name" "bobibm" "block_on_workplace" "Zup") (dict "name" "claude")) -}}{{- $blocked := false -}}{{- if and (hasKey . "block_on_workplace") (eq .block_on_workplace "none") }}{{ $blocked = true }}{{- end -}}{{ if not $blocked }}INSTALL {{ .name }}
    {{ end }}{{- end -}}'
    OUT_NONE="$(printf '%s' "$SNIP" | chezmoi execute-template)"
    echo "$OUT_NONE" | grep -q 'INSTALL bobibm' || { echo "FAIL: bobibm bloqueado em workplace none"; exit 1; }
    echo "$OUT_NONE" | grep -q 'INSTALL claude' || { echo "FAIL: claude bloqueado em none"; exit 1; }

    echo "--- 5) sem regressao: tools sem o campo instalam em Zup ---"
    echo "$OUT_REAL" | grep -q 'Installing aider (Curl)' || { echo "FAIL: aider sumiu"; exit 1; }
    echo "$OUT_REAL" | grep -q 'Installing opencode (NPM)' || { echo "FAIL: opencode sumiu"; exit 1; }

    echo "ALL CHECKS PASSED"

Notas de implementação do script de verificação:
- `chezmoi execute-template < "$F"` usa os dados reais da máquina (workplace = Zup hoje), então
  a checagem 3 prova diretamente a exclusão de bobibm em Zup.
- A checagem 4 NÃO altera o workplace da máquina; ela renderiza um snippet inline com a MESMA
  lógica de guard (`hasKey` + `eq`) contra um workplace `none` literal, provando a inclusão.
- `grep -c` é seguro aqui: os tokens `block_on_workplace` e `and $featureEnabled (not $blocked)`
  só aparecem nas linhas de lógica adicionadas (o arquivo não tem comentários com esses tokens).
</verification>

<success_criteria>
- `home/.chezmoidata/ai_tools.yaml` válido com `block_on_workplace: Zup` apenas em `bobibm`.
- Script renderiza sem erro via `chezmoi execute-template`.
- Em Zup: bobibm ausente; demais tools (npm e curl) presentes.
- Em não-Zup (simulado): bobibm presente.
- Guard genérico (campo declarativo), aplicado a ambos os ranges; sem hardcode de bobibm.
- `verify-task2.sh` imprime `ALL CHECKS PASSED`.
</success_criteria>

<output>
Create `.planning/quick/260603-669-ia-tools-ibm-bob-skip-zup/260603-669-SUMMARY.md` when done
</output>

features:
  idea:
    # Controle por OS — cada script checa a propria chave
    install:
      linux: true
      darwin: true
      windows: false
    # Versao fixa (vazio = mais recente do GitHub)
    # Exemplo: "2026.1.2"
    version: "2025.3.1.1"
    # Filtro adicional por workplace (vazio = todos)
    # workplaces:
    #   - Zup
    #   - Instivo
    workplaces: []
